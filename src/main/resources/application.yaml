spring:
  datasource:
    url: jdbc:h2:file:./db/filmorate
    username: sa
    password: password
    driver-class-name: org.h2.Driver

  sql:
    init:
      mode: always
      platform: h2
      encoding: UTF-8

  h2:
    console:
      enabled: true
      path: /h2-console
